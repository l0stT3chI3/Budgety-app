
document.querySelector('.ty').addEventListener('click', function () {

    var html, vas, sd;
    vas = document.querySelector('.addS').value;
    html = '<div class="gt">h</div>';
    newHTML = html.replace('h', vas);
    document.querySelector('.all').insertAdjacentHTML('beforeend', newHTML);
    sd = document.querySelector('.addS');
    ff = array.prototype.slice.call(sd);
    ff.array.forEach(current) {
        current = "";
    });




});